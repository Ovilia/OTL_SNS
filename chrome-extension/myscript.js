function click() {
	        //chrome.tabs.executeScript(null,{code:"document.body.style.backgroundColor='" + color.id + "'"});
	        //window.close();
	        //chrome.tabs.executeScript(null,{code:"alert('123');"});
	        /*var html = $.ajax({ url: "http://www.baidu.com", async: false }).responseText;
			        $.ajax({
						            type: "POST",
						            url: "localhost/test.php",
						            data: ("text=" + html),
						            success: function{chrome.tabs.executeScript(null,{code:"alert('ok');"});},
						            error: function{}
					        });
					        //chrome.tabs.executeScript(null,{file:"getClass.js"});
			*/
	alert('123');
}
